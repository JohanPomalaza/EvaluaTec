<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.LoginApp" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="LoginButton" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
        <item name="backgroundTint">@color/blue_primary</item>
        <item name="cornerRadius">8dp</item>
        <item name="android:paddingVertical">12dp</item>
    </style>

    <style name="TextAppearance.BodySmall" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">?attr/colorOnSurfaceVariant</item>
    </style>

    <style name="BottomNavText" parent="TextAppearance.Material3.LabelSmall">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">?attr/colorOnSurface</item>
    </style>

    <style name="AnimatedCardStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">4dp</item>
        <item name="strokeColor">@color/blue_primary</item>
        <item name="android:clickable">true</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintWidth_percent">0.95</item>
        <item name="android:padding">20dp</item>
    </style>

    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="android:gravity">center</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:height">60dp</item>
    </style>

    <!-- Blue Button Style -->
    <style name="Widget.App.BlueButton" parent="Widget.App.Button">
        <item name="android:background">@drawable/blue_button_background</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Green Button Style -->
    <style name="Widget.App.GreenButton" parent="Widget.App.Button">
        <item name="android:background">@drawable/green_button_background</item>
        <item name="android:textColor">@android:color/white</item>
    </style>
</resources>